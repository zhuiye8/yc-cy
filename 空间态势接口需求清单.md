# 空间态势接口需求清单

## 0. 边界说明

空间态势页面里有两类能力：

### 前端本地可以处理

- 行政区索引：全国、省、市、区县的 `adcode / name / parentCode / center / children`。
- GeoJSON：全国、省、市边界数据。
- 地图下钻逻辑：全国 → 省 → 市/区县。
- 技术领域/赛道字典：例如 `robot`、`bio`、`energy`、`ai`。

这些不需要后端先提供接口。前端可以本地维护，后续如需后台动态维护再单独升级。

### 后端需要提供

后端重点提供“数据接口”，即按区域、类型、领域返回真实数据：

- 人才
- 企业
- 论文
- 专利
- 统计概览
- 趋势
- 排名
- 情报动态

建议后端统一使用 `/api/space/*` 命名空间，避免和 yc 项目的 `/api/chain-*` 产业链接口语义冲突。

## 1. 空间态势区域统计接口

应用场景：全国、省、市态势概览，地图颜色深浅，顶部指标，粒子数量，脉冲区域。

建议新增接口：`GET /api/space/statistics`

请求参数：
- `regionCode`：选填，行政区编码。为空或 `100000` 表示全国。
- `level`：选填，返回聚合粒度。可选值：`province`、`city`、`district`。例如全国看省级，省内看城市级。
- `domain`：选填，技术领域/赛道，例如 `robot`、`bio`、`energy`、`ai`。
- `category`：选填，数据类型。可选值：`Talent`、`Enterprise`、`Paper`、`Patent`。
- `startDate` / `endDate`：选填，统计时间范围。

返回要求：
- `summary`：当前范围总览。
- `items`：区域聚合数组。
- `items[].regionCode`：行政区编码。
- `items[].regionName`：行政区名称。
- `items[].score`：综合指数，用于地图着色。
- `items[].pulse`：是否作为脉冲高亮区域。
- `items[].counts`：四类数据数量。

请求示例：
```http
GET /api/space/statistics?regionCode=100000&level=province&domain=ai
```

响应示例：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "summary": {
      "nodeTotal": 28640,
      "pulseRegionTotal": 12,
      "talentTotal": 9200,
      "enterpriseTotal": 7140,
      "paperTotal": 6800,
      "patentTotal": 5500
    },
    "items": [
      {
        "regionCode": "420000",
        "regionName": "湖北省",
        "score": 86,
        "pulse": true,
        "counts": {
          "Talent": 912,
          "Enterprise": 634,
          "Paper": 518,
          "Patent": 322
        }
      }
    ]
  }
}
```

前端使用方式：
- 全国视图：`regionCode=100000&level=province`。
- 省级视图：`regionCode=420000&level=city`。
- 城市视图：`regionCode=420100&level=district`。
- 前端本地行政区字典负责补齐坐标和边界，接口只负责真实统计数据。

## 2. 区域人才列表接口

应用场景：城市/区域中生成人才粒子；点击人才粒子前展示人才卡片；后续可作为人才搜索列表。

建议新增接口：`GET /api/space/talents`

请求参数：
- `regionCode`：必填，行政区编码。
- `domain`：选填，技术领域/赛道。
- `keyword`：选填，关键词。
- `page`：选填，默认 `1`。
- `pageSize`：选填，默认 `20`。
- `sort`：选填，默认 `score_desc`。

返回要求：
- `total`、`page`、`pageSize`。
- `items[].id`：人才 ID。
- `items[].name`：姓名。
- `items[].title`：展示标题，例如研究方向/人才称号。
- `items[].subtitle`：机构或团队。
- `items[].score`：综合指数。
- `items[].trend`：趋势。
- `items[].tags`：标签。
- `items[].regionCode` / `regionName`。
- `items[].coordinate`：可选，经纬度。没有真实坐标时可为空。

请求示例：
```http
GET /api/space/talents?regionCode=420100&domain=ai&page=1&pageSize=20
```

响应示例：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 128,
    "page": 1,
    "pageSize": 20,
    "items": [
      {
        "id": "talent-001",
        "name": "沈明",
        "title": "光电与智能感知带头人",
        "subtitle": "华中科技大学光电团队",
        "score": 96,
        "trend": "+11%",
        "tags": ["人才引领", "论文 22 篇"],
        "regionCode": "420100",
        "regionName": "武汉市",
        "coordinate": [114.305392, 30.593098]
      }
    ]
  }
}
```

前端使用方式：
- 生成 `Talent` 类型粒子。
- 点击人才粒子后，用 `id` 调人才详情接口。

## 3. 区域企业列表接口

应用场景：城市/区域中生成企业粒子；展示企业卡片；支撑企业类空间态势。

建议新增接口：`GET /api/space/enterprises`

请求参数：
- `regionCode`：必填，行政区编码。
- `domain`：选填，技术领域/赛道。
- `keyword`：选填，关键词。
- `tags`：选填，企业标签。
- `page`：选填，默认 `1`。
- `pageSize`：选填，默认 `20`。
- `sort`：选填，默认 `score_desc`。

返回要求：
- `total`、`page`、`pageSize`。
- `items[].id`：企业 ID。
- `items[].name`：企业名称。
- `items[].title`：展示标题，例如企业类型或核心能力。
- `items[].subtitle`：补充说明。
- `items[].score`：综合指数。
- `items[].trend`：趋势。
- `items[].tags`：标签。
- `items[].regionCode` / `regionName`。
- `items[].coordinate`：可选，经纬度。

请求示例：
```http
GET /api/space/enterprises?regionCode=420100&domain=robot&page=1&pageSize=20
```

## 4. 区域论文列表接口

应用场景：城市/区域中生成论文粒子；展示论文卡片；支撑论文类空间态势。

建议新增接口：`GET /api/space/papers`

请求参数：
- `regionCode`：必填，行政区编码。
- `domain`：选填，技术领域/赛道。
- `keyword`：选填，关键词。
- `yearStart` / `yearEnd`：选填，发表年份范围。
- `page`：选填，默认 `1`。
- `pageSize`：选填，默认 `20`。
- `sort`：选填，默认 `score_desc`。

返回要求：
- `total`、`page`、`pageSize`。
- `items[].id`：论文 ID。
- `items[].title`：论文标题。
- `items[].name`：短名称，用于粒子卡片。
- `items[].subtitle`：作者或机构。
- `items[].score`：综合指数。
- `items[].trend`：趋势。
- `items[].tags`：关键词/期刊/被引标签。
- `items[].regionCode` / `regionName`。
- `items[].coordinate`：可选，经纬度。

请求示例：
```http
GET /api/space/papers?regionCode=420100&domain=bio&page=1&pageSize=20
```

## 5. 区域专利列表接口

应用场景：城市/区域中生成专利粒子；展示专利卡片；支撑专利类空间态势。

建议新增接口：`GET /api/space/patents`

请求参数：
- `regionCode`：必填，行政区编码。
- `domain`：选填，技术领域/赛道。
- `keyword`：选填，关键词。
- `patentType`：选填，专利类型。
- `legalStatus`：选填，法律状态。
- `yearStart` / `yearEnd`：选填，申请年份范围。
- `page`：选填，默认 `1`。
- `pageSize`：选填，默认 `20`。
- `sort`：选填，默认 `score_desc`。

返回要求：
- `total`、`page`、`pageSize`。
- `items[].id`：专利 ID。
- `items[].title`：专利标题。
- `items[].name`：短名称，用于粒子卡片。
- `items[].subtitle`：申请人/状态。
- `items[].score`：综合指数。
- `items[].trend`：趋势。
- `items[].tags`：IPC、专利类型、法律状态等标签。
- `items[].regionCode` / `regionName`。
- `items[].coordinate`：可选，经纬度。

请求示例：
```http
GET /api/space/patents?regionCode=420100&domain=ai&page=1&pageSize=20
```

## 6. 人才详情接口

应用场景：点击人才粒子后打开右侧详情面板，展示人才关系、研究方向、论文/专利趋势、近期动态。

建议新增接口：`GET /api/space/talents/{id}`

返回要求：
- `base`：人才基础信息。
- `metrics`：KPI 卡片，例如发表论文、授权专利、H 指数、合作机构。
- `graph.nodes` / `graph.links`：人才关系图。
- `directions`：研究方向雷达图。
- `pubTrend`：论文/专利年度趋势。
- `activities`：近期动态。

请求示例：
```http
GET /api/space/talents/talent-001
```

## 7. 企业详情接口

应用场景：点击企业粒子后打开右侧详情面板，展示企业技术标签、人才类型、产出趋势、合作伙伴、近期动态。

建议新增接口：`GET /api/space/enterprises/{id}`

返回要求：
- `base`：企业基础信息。
- `metrics`：KPI 卡片。
- `techTags`：技术标签云。
- `talentTypes`：人才类型分布。
- `outputTrend`：论文/专利等产出趋势。
- `partners` / `partnerLinks`：合作伙伴关系图。
- `activities`：近期动态。

请求示例：
```http
GET /api/space/enterprises/enterprise-001
```

## 8. 论文详情接口

应用场景：点击论文粒子后打开右侧详情面板，展示引用网络、学科分布、关键词、相关论文、作者。

建议新增接口：`GET /api/space/papers/{id}`

返回要求：
- `base`：论文基础信息。
- `metrics`：KPI 卡片，例如被引、下载、影响因子等。
- `citationNodes` / `citationLinks`：引用网络。
- `domains`：学科分布。
- `keywords`：关键词权重。
- `relatedPapers`：相关论文。
- `authors`：作者列表。

请求示例：
```http
GET /api/space/papers/paper-001
```

## 9. 专利详情接口

应用场景：点击专利粒子后打开右侧详情面板，展示技术分类树、申请趋势、申请主体网络、IPC 分布、近期动态。

建议新增接口：`GET /api/space/patents/{id}`

返回要求：
- `base`：专利基础信息。
- `metrics`：KPI 卡片，例如专利数量、授权占比、申请人、IPC 类别。
- `techTree`：技术分类树。
- `filingTrend`：申请趋势。
- `claimants` / `claimantLinks`：申请主体关系图。
- `ipcDist`：IPC 分布。
- `activities`：近期动态。

请求示例：
```http
GET /api/space/patents/patent-001
```

## 10. 情报动态详情接口

应用场景：在人才、企业、论文、专利详情面板中点击论文、专利、项目、奖项等动态，打开详情弹窗。

建议新增接口：`GET /api/space/activities/{activityId}`

请求参数：
- `activityId`：必填，动态 ID。

返回要求：
- `kindLabel`：详情类型，例如 `论文详情`、`专利详情`、`项目详情`。
- `title`：标题。
- `date`：日期。
- `publisherLabel`：发布主体字段名，例如 `作者机构`、`申请主体`。
- `publisher`：发布主体。
- `codeLabel`：编号字段名，例如 `DOI`、`公开号`。
- `code`：编号。
- `summary`：摘要。
- `people`：人员/申请人/作者信息数组。
- `highlights`：要点数组。
- `tags`：标签数组。
- `description`：详细说明。
- `analysis`：情报分析。

请求示例：
```http
GET /api/space/activities/act-001
```

## 11. 空间态势排名接口

应用场景：全国态势或省级态势中展示 Top 省份、Top 城市、Top 人才、Top 企业、Top 论文、Top 专利。

建议新增接口：`GET /api/space/rankings`

请求参数：
- `regionCode`：选填，默认全国。
- `rankType`：必填。可选值：`region`、`talent`、`enterprise`、`paper`、`patent`。
- `domain`：选填，技术领域/赛道。
- `limit`：选填，默认 `10`。
- `startDate` / `endDate`：选填。

返回要求：
- `items[].rank`：排名。
- `items[].id`：实体 ID。
- `items[].name`：名称。
- `items[].regionCode` / `regionName`：所属区域。
- `items[].value`：主数值。
- `items[].score`：综合指数。
- `items[].trend`：趋势。
- `items[].tags`：标签。

请求示例：
```http
GET /api/space/rankings?regionCode=100000&rankType=enterprise&domain=robot&limit=10
```

## 12. 空间态势趋势接口

应用场景：区域、领域、类型维度的趋势图。

建议新增接口：`GET /api/space/trends`

请求参数：
- `regionCode`：选填，默认全国。
- `category`：选填，`Talent`、`Enterprise`、`Paper`、`Patent`。
- `domain`：选填，技术领域/赛道。
- `granularity`：选填，`month`、`quarter`、`year`，默认 `month`。
- `startDate` / `endDate`：选填。

返回要求：
- `xAxis`：时间轴。
- `series`：多序列数据。
- `series[].name`：序列名称。
- `series[].data`：数值数组。

请求示例：
```http
GET /api/space/trends?regionCode=100000&domain=robot&category=Enterprise&granularity=month
```

## 13. 空间态势搜索接口

应用场景：搜索某个人才、企业、论文、专利，并在地图上定位。

建议新增接口：`GET /api/space/search`

请求参数：
- `keyword`：必填，检索关键词。
- `category`：选填，`Talent`、`Enterprise`、`Paper`、`Patent`。
- `domain`：选填，技术领域/赛道。
- `regionCode`：选填，行政区编码。
- `page`：选填，默认 `1`。
- `pageSize`：选填，默认 `20`。

返回要求：
- `total`、`page`、`pageSize`。
- `items[]` 结构与对应类型列表接口一致。

请求示例：
```http
GET /api/space/search?keyword=视觉检测&category=Patent&regionCode=420100&page=1&pageSize=20
```

## 14. 数据字典和口径要求

### 14.1 情报类型字典

固定四类：
- `Talent`：人才
- `Enterprise`：企业
- `Paper`：论文
- `Patent`：专利

### 14.2 技术领域/赛道字典

当前项目先使用：
- `robot`：机器人
- `bio`：生物医药
- `energy`：新能源
- `ai`：人工智能

注意：这里不是 yc 项目的产业链名称，也不要求兼容 yc 的链结构。

### 14.3 行政区口径

请求参数统一使用 `regionCode`，即国家标准行政区划码。

后端响应中如果返回区域名称，建议同时返回：
- `regionCode`
- `regionName`
- `regionLevel`

坐标和边界由前端本地行政区字典处理，后端数据接口可以不返回 GeoJSON。

### 14.4 时间口径

所有统计接口建议支持：
- `startDate`
- `endDate`

响应中建议返回：
- `statDate`
- `updatedAt`
- `dataVersion`

## 15. 接入优先级

### P0：空间态势能接入真实数据

1. `GET /api/space/statistics`
2. `GET /api/space/talents`
3. `GET /api/space/enterprises`
4. `GET /api/space/papers`
5. `GET /api/space/patents`
6. `GET /api/space/talents/{id}`
7. `GET /api/space/enterprises/{id}`
8. `GET /api/space/papers/{id}`
9. `GET /api/space/patents/{id}`

### P1：详情和交互完整

10. `GET /api/space/activities/{activityId}`
11. `GET /api/space/rankings`
12. `GET /api/space/trends`

### P2：增强能力

13. `GET /api/space/search`

## 16. 现有 Swagger 接口复用评估

调研地址：`http://119.36.242.222:19020/swagger-ui/index.html`

服务标题为 `TalentGraphService`。现有接口主要分为 `Talent`、`Paper`、`Patents`、`Stat`、`ES`、`ChainTalent`、`ChainOrg`、`YichangTalent` 等分组。

本项目和 yc 项目不是同一个项目，产业链模型也不兼容。因此以下评估只判断接口能力是否能为本项目空间态势提供数据支撑，不把 yc 的产业链口径作为本项目口径。

### 16.1 可以直接复用或通过前端适配复用

#### 人才列表

现有接口：`GET /api/talents/search`

可用范围：
- 可用于本项目 `GET /api/space/talents` 的第一版数据来源。
- 支持 `keyword`、`province`、`city`、`page`、`pageSize`、`sort`。
- 返回字段包含 `ID`、`CNAME`、`AORG`、`TITLE`、`DIRECTION`、`QIKAN`、`ZHUANLI`、`CHENGGUO`、`PROVINCE`、`CITY`、`TAGLARG`、`PHOTOPATH`。

需要适配：
- 本项目前端需要统一成 `id`、`name`、`title`、`subtitle`、`score`、`trend`、`tags`、`regionCode`、`regionName`。
- 现有接口按 `province/city` 过滤，不是统一 `regionCode`。
- 现有接口没有坐标字段，粒子落点仍需要前端按区域中心或散点算法生成。
- `domain` 可以先映射到 `keyword`，但长期建议后端提供明确的 `domain` 参数。

#### 人才详情

现有接口：`GET /api/talents/{id}`

可用范围：
- 可用于本项目 `GET /api/space/talents/{id}`。
- 返回字段包含姓名、机构、职称、方向、简介、摘要、地域、论文数、专利数、成果数、联系方式等。

需要适配：
- 字段命名需要转换为本项目统一详情结构。
- 联系方式类字段是否展示要按权限控制。

#### 人才产出统计

现有接口：`GET /api/talents/{id}/output-stats`

可用范围：
- 可用于人才详情卡片里的论文、专利、成果统计。
- 返回 `QIKAN`、`ZHUANLI`、`CHENGGUO`。

需要适配：
- 本项目空间态势里如果只展示人才详情统计，可以直接使用。
- 如果要做区域级论文/专利统计，不能只依赖该接口。

#### 人才关系图

现有接口：`GET /api/talents/{id}/graph`

可用范围：
- 可用于人才详情中的关系网络、合作关系展示。
- 支持 `level`、`nodeCount`。

需要适配：
- 与当前 3D 空间态势粒子主图不是同一类数据，但可用于详情弹层或下钻面板。

#### 合作机构和合作人才

现有接口：
- `GET /api/talents/{id}/cooperate-orgs`
- `GET /api/talents/{id}/coauthors`

可用范围：
- 可用于人才详情里的合作机构、合作人列表。

需要适配：
- 只适合人才详情，不适合区域企业、区域人才主列表。

### 16.2 可以改造后复用

#### 人才区域聚合

现有接口：`GET /api/stats/region-aggregation`

可改造为：
- 本项目 `GET /api/space/statistics` 的人才维度来源。

现有能力：
- 支持 `keyword`、`ckey`、`province`、`city`、`areacode`、`size`。
- 返回 `total` 和 `items[]`，每项为 `name/value`。

缺口：
- 目前语义是人才区域聚合，只覆盖人才。
- 返回项缺少 `regionCode`、`regionLevel`、`score`、`counts`。
- 不能一次返回人才、企业、论文、专利四类统计。

建议改造：
- 新增或封装为 `GET /api/space/statistics`。
- 入参统一为 `regionCode`、`level`、`domain`、`category`、`startDate`、`endDate`。
- 返回统一结构：`summary` + `items[].regionCode/regionName/score/counts`。

#### 年度趋势

现有接口：`GET /api/stats/year-trend`

可改造为：
- 本项目 `GET /api/space/trends` 的一部分。

现有能力：
- 支持 `id` 或 `keyword`。
- 返回 `years`、`papers`、`patents`、`standards`。

缺口：
- 没有 `regionCode`。
- 没有企业趋势。
- 返回的是论文/专利/标准，不完全等同于本项目四类空间态势 `Talent/Enterprise/Paper/Patent`。

建议改造：
- 增加 `regionCode`、`domain`、`category`。
- 返回按类别拆分的趋势序列。

#### 论文列表

现有接口：`GET /api/papers/list`

现有能力：
- 按人才 `id` 查询论文列表。
- 返回 `id`、`title`、`publishYear`、`journal`、`authors`、`doi`。

缺口：
- 只能按人才 ID 查，不能按全国/省/市/领域查询。
- 不能直接作为空间态势里的区域论文粒子列表。

建议改造：
- 新增 `GET /api/space/papers`，支持 `regionCode`、`domain`、`keyword`、`yearStart`、`yearEnd`、分页。
- 保留现有 `/api/papers/list` 用于人才详情里的论文产出明细。

#### 专利列表

现有接口：`GET /api/patents/list`

现有能力：
- 按人才 `id` 查询专利列表。
- 返回 `id`、`title`、`applyDate`、`patentType`、`status`、`patentNo`。

缺口：
- 只能按人才 ID 查，不能按全国/省/市/领域查询。
- 不能直接作为空间态势里的区域专利粒子列表。

建议改造：
- 新增 `GET /api/space/patents`，支持 `regionCode`、`domain`、`keyword`、`yearStart`、`yearEnd`、分页。
- 保留现有 `/api/patents/list` 用于人才详情里的专利产出明细。

#### 统一 ES 查询

现有接口：`POST /api/es/query`

可用范围：
- 如果后端已经把人才、企业、论文、专利索引纳入白名单，可以作为后端聚合接口的底层数据源。

不建议：
- 不建议空间态势前端直接调用 ES 查询接口。

建议改造：
- 由后端用 ES 查询封装 `/api/space/*` 接口。
- 前端只面对稳定业务接口，不感知 ES 索引、字段和 DSL。

### 16.3 不建议复用

#### yc 人才库接口

现有接口：`GET /api/yichang-talents/search`

不建议原因：
- 接口语义固定为宜昌人才库。
- 本项目需要全国空间态势，不能绑定宜昌口径。

#### 产业链人才接口

现有接口：
- `GET /api/chain-talents/search`
- `GET /api/chain-talents/province-distribution`
- `GET /api/chain-talents/city-distribution`
- `GET /api/chain-talents/year-trend`

不建议原因：
- 强依赖 `chain` 参数和 yc 产业链名称。
- 本项目产业链模型不兼容，不能直接使用。

可借鉴点：
- 如果后端要做本项目领域维度聚合，可以参考这些接口的实现方式，但接口语义和入参必须重新定义为本项目的 `domain/category/regionCode`。

#### 产业链企业接口

现有接口：
- `GET /api/chain-orgs/search`
- `GET /api/chain-orgs/province-distribution`
- `GET /api/chain-orgs/city-distribution`
- `GET /api/chain-orgs/node-org-counts`
- `GET /api/chain-orgs/coverage-summary`

不建议原因：
- 强依赖 `chain` 参数。
- 返回字段里有大量产业链匹配字段，例如 `root_chain_name`、`matched_leaf_nodes`、`matched_keywords`、`matched_query_strings`。
- 本项目空间态势需要的是企业空间分布和企业详情，不是 yc 产业链覆盖分析。

可改造方向：
- 不复用接口本身。
- 可以复用底层企业索引查询能力，新增 `GET /api/space/enterprises` 和 `GET /api/space/enterprises/{id}`。

#### 产业链总量统计

现有接口：`GET /api/stats/industry-chain-total`

不建议原因：
- 接口语义是产业链总量统计。
- 只返回 `enterpriseTotal`、`talentTotal`、`standardTotal`，缺少本项目需要的论文、专利、区域聚合、类别分布。

### 16.4 当前 Swagger 对本项目的覆盖结论

| 本项目需要 | 现有 Swagger 覆盖情况 | 建议 |
| --- | --- | --- |
| 区域统计 `statistics` | 只覆盖人才区域聚合的一部分 | 改造 `/api/stats/region-aggregation` 或新建 `/api/space/statistics` |
| 人才列表 `talents` | 基本可用 | 用 `/api/talents/search` 封装适配 |
| 人才详情 `talents/{id}` | 基本可用 | 用 `/api/talents/{id}` 封装适配 |
| 企业列表 `enterprises` | 只有产业链企业搜索，不兼容 | 新建 `/api/space/enterprises` |
| 企业详情 `enterprises/{id}` | 未看到通用企业详情 | 新建 |
| 论文列表 `papers` | 只有按人才 ID 的论文列表 | 新建区域/领域论文列表 |
| 论文详情 `papers/{id}` | 未看到论文详情 | 新建或补充 |
| 专利列表 `patents` | 只有按人才 ID 的专利列表 | 新建区域/领域专利列表 |
| 专利详情 `patents/{id}` | 未看到专利详情 | 新建或补充 |
| 趋势 `trends` | 有论文/专利/标准年度趋势 | 改造支持 `regionCode/domain/category` |
| 排名 `rankings` | 未看到通用排名接口 | 新建 |
| 动态 `activities` | 未看到情报动态接口 | 新建 |
| 统一搜索 `search` | ES 查询可作为底层能力 | 后端封装业务接口，不建议前端直连 ES |

### 16.5 接入建议

第一阶段优先接：
1. `/api/talents/search` → 封装为 `/api/space/talents`
2. `/api/talents/{id}` → 封装为 `/api/space/talents/{id}`
3. `/api/talents/{id}/output-stats` → 用于人才详情统计
4. `/api/stats/region-aggregation` → 先支撑人才空间分布

第一阶段同时需要后端新增：
1. `/api/space/statistics`
2. `/api/space/enterprises`
3. `/api/space/papers`
4. `/api/space/patents`

第二阶段再补：
1. `/api/space/enterprises/{id}`
2. `/api/space/papers/{id}`
3. `/api/space/patents/{id}`
4. `/api/space/rankings`
5. `/api/space/trends`
6. `/api/space/activities/{activityId}`
7. `/api/space/search`
