# 空间态势 P0 接口协议

## 1. 统一约定

### 1.1 接口前缀

```http
/api/space/*
```

### 1.2 响应格式

```json
{
  "code": "0",
  "message": "OK",
  "data": {}
}
```

### 1.3 实体分类

| category | 页面含义 |
| --- | --- |
| `Talent` | 人才 |
| `Enterprise` | 企业 |
| `Paper` | 论文 |
| `Patent` | 专利 |

### 1.4 地域字段

| 字段 | 说明 |
| --- | --- |
| `regionCode` | 行政区编码，建议使用国家标准 adcode |
| `regionName` | 行政区名称 |
| `parentRegionCode` | 上级行政区编码 |
| `level` | `country` / `province` / `city` / `district` |

`regionCode` 是推荐字段。若后端暂时无法返回，可先返回 `province`、`city`、`district`，前端用本地行政区索引反查 adcode。

## 2. 区域统计

```http
GET /api/space/regions/summary
```

用于地图着色、顶部统计、全国/省/市下钻后的区域数据。

### 2.1 请求参数

| 参数 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `regionCode` | 是 | string | `100000` | 当前地图范围。全国传 `100000`，省传省级 adcode，市传市级 adcode |
| `level` | 是 | string | `province` | 返回下一级区域：`province` / `city` / `district` |
| `category` | 否 | string | `Enterprise` | 指定某类统计；为空则返回四类汇总 |

### 2.2 响应结构

```json
{
  "code": "0",
  "message": "OK",
  "data": {
    "regionCode": "100000",
    "regionName": "全国",
    "level": "province",
    "summary": {
      "talentTotal": 12800,
      "enterpriseTotal": 6200,
      "paperTotal": 18000,
      "patentTotal": 52000
    },
    "items": [
      {
        "regionCode": "420000",
        "regionName": "湖北省",
        "parentRegionCode": "100000",
        "level": "province",
        "value": 8620,
        "counts": {
          "Talent": 820,
          "Enterprise": 430,
          "Paper": 1260,
          "Patent": 3600
        }
      }
    ]
  }
}
```

### 2.3 字段说明

| 字段 | 说明 |
| --- | --- |
| `summary` | 当前范围内四类实体总量 |
| `items` | 下一级行政区列表 |
| `items[].value` | 地图着色值。若 `category` 为空，可用四类总和；若指定 `category`，则为该类数量 |
| `items[].counts` | 四类实体数量 |

### 2.4 可参考现有接口

| 现有 Swagger 接口 | 可参考点 | 需要补齐 |
| --- | --- | --- |
| `GET /api/stats/region-aggregation` | 区域聚合口径 | 返回值需要补 `regionCode` |
| `GET /api/chain-talents/province-distribution` | 人才省级分布 | 字段统一为 `regionCode/regionName/value` |
| `GET /api/chain-talents/city-distribution` | 人才城市分布 | 字段统一为 `regionCode/regionName/value` |
| `GET /api/chain-orgs/province-distribution` | 企业省级分布 | 字段统一为 `regionCode/regionName/value` |
| `GET /api/chain-orgs/city-distribution` | 企业城市分布 | 字段统一为 `regionCode/regionName/value` |

## 3. 区域情报列表

```http
GET /api/space/regions/{regionCode}/intel
```

用于点击地图区域后，返回当前分类的粒子卡片列表。

### 3.1 请求参数

| 参数 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `regionCode` | 是 | path string | `420100` | 当前区域编码 |
| `category` | 是 | string | `Enterprise` | `Talent` / `Enterprise` / `Paper` / `Patent` |
| `page` | 否 | number | `1` | 页码 |
| `pageSize` | 否 | number | `20` | 每页条数 |

### 3.2 响应结构

```json
{
  "code": "0",
  "message": "OK",
  "data": {
    "regionCode": "420100",
    "regionName": "武汉市",
    "category": "Enterprise",
    "page": 1,
    "pageSize": 20,
    "total": 128,
    "items": [
      {
        "id": "org-001",
        "category": "Enterprise",
        "name": "武汉智造云谷科技有限公司",
        "title": "智能制造链主企业",
        "subtitle": "湖北省 / 武汉市",
        "value": 93,
        "trend": "+10%",
        "tags": ["高新技术企业", "专利 16 项"],
        "regionCode": "420100",
        "regionName": "武汉市",
        "sourceFields": {
          "ORGID": "ORG001",
          "AREACODE": "420100"
        }
      }
    ]
  }
}
```

### 3.3 字段说明

| 字段 | 页面用途 |
| --- | --- |
| `id` | 点击粒子后请求详情 |
| `category` | 决定使用哪类详情模板 |
| `name` | 卡片主标题 |
| `title` | 卡片副标题 |
| `subtitle` | 地域、机构、申请主体等补充信息 |
| `value` | 粒子强度/热度值 |
| `trend` | 增长趋势，可为空 |
| `tags` | 卡片标签 |
| `sourceFields` | 可选，保留原始字段，方便排查 |

### 3.4 可参考现有接口

| category | 现有 Swagger 接口 | 字段参考 |
| --- | --- | --- |
| `Talent` | `GET /api/talents/search` | `ID`、`CNAME`、`AORG`、`TITLE`、`H`、`QIKAN`、`ZHUANLI`、`CHENGGUO`、`PROVINCE`、`CITY`、`TAGLARG`、`PHOTOPATH` |
| `Enterprise` | `GET /api/chain-orgs/search` | `ORGID`、`ID`、`NAME`、`PROV`、`CITY`、`AREACODE`、`COMPANYTYPE`、`TAGS`、`TAGLARG`、`QIKAN`、`ZHUANLI`、`CHENGGUO` |
| `Paper` | `GET /api/papers/list` | `id`、`title`、`publishYear`、`journal`、`authors`、`doi` |
| `Patent` | `GET /api/patents/list` | `id`、`title`、`applyDate`、`patentType`、`status`、`patentNo` |

`Paper` 和 `Patent` 现有接口主要按人才 `id` 查询。若要支撑空间态势区域列表，需要后端增加按 `regionCode` 查询的能力。

## 4. 情报详情

```http
GET /api/space/intel/{itemId}
```

用于点击粒子后展示右侧详情面板。

### 4.1 请求参数

| 参数 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `itemId` | 是 | path string | `talent-001` | 列表接口返回的 `id` |
| `category` | 是 | string | `Talent` | `Talent` / `Enterprise` / `Paper` / `Patent` |

### 4.2 通用响应结构

```json
{
  "code": "0",
  "message": "OK",
  "data": {
    "id": "talent-001",
    "category": "Talent",
    "name": "任波",
    "title": "教授 / 博士生导师",
    "subtitle": "武汉智能感知科技有限公司",
    "summary": "长期从事机器视觉、工业检测与智能制造领域研究。",
    "metrics": [
      { "label": "论文", "value": "128" },
      { "label": "专利", "value": "28" }
    ],
    "detail": {}
  }
}
```

`detail` 按 `category` 返回不同结构。

### 4.3 Talent detail

```json
{
  "graphNodes": [
    { "id": "core", "label": "任波", "nodeType": "Talent" }
  ],
  "graphLinks": [
    { "sourceId": "core", "targetId": "org-001", "relationType": "任职" }
  ],
  "directions": [
    { "name": "机器视觉", "value": 88, "delta": "+12%" }
  ],
  "pubTrend": {
    "years": [2021, 2022, 2023, 2024],
    "papers": [8, 11, 14, 18],
    "patents": [2, 3, 4, 6]
  },
  "activities": [
    {
      "id": "paper-001",
      "type": "Paper",
      "title": "复杂表面缺陷检测方法研究",
      "date": "2024-03-15"
    }
  ]
}
```

可参考现有接口：

- `GET /api/talents/{id}`
- `GET /api/talents/{id}/graph`
- `GET /api/talents/{id}/output-stats`
- `GET /api/talents/{id}/coauthors`
- `GET /api/talents/{id}/cooperate-orgs`

### 4.4 Enterprise detail

```json
{
  "techTags": [
    { "name": "智能制造", "value": 92 }
  ],
  "talentTypes": [
    { "name": "研发人才", "value": 42 }
  ],
  "outputTrend": {
    "years": [2021, 2022, 2023, 2024],
    "papers": [3, 5, 7, 9],
    "patents": [5, 8, 12, 16]
  },
  "partners": [
    { "id": "org-002", "name": "华中科技大学", "value": 18 }
  ],
  "partnerLinks": [
    { "sourceId": "org-001", "targetId": "org-002", "relationType": "合作" }
  ],
  "activities": []
}
```

可参考现有接口：

- `GET /api/chain-orgs/search`

需要后端补企业详情数据，或由 `/api/space/intel/{itemId}?category=Enterprise` 聚合返回。

### 4.5 Paper detail

```json
{
  "citationNodes": [
    { "id": "paper-001", "label": "当前论文", "nodeType": "Paper" }
  ],
  "citationLinks": [],
  "domains": [
    { "name": "机器视觉", "value": 88 }
  ],
  "keywords": [
    { "name": "缺陷检测", "value": 32 }
  ],
  "authors": [
    { "id": "talent-001", "name": "任波", "orgName": "华中科技大学" }
  ],
  "relatedPapers": []
}
```

可参考现有接口：

- `GET /api/papers/list`

需要后端补论文详情数据，或由 `/api/space/intel/{itemId}?category=Paper` 聚合返回。

### 4.6 Patent detail

```json
{
  "techTree": {
    "name": "专利技术构成",
    "children": [
      { "name": "图像采集", "value": 35 }
    ]
  },
  "filingTrend": {
    "years": [2021, 2022, 2023, 2024],
    "values": [3, 6, 9, 12]
  },
  "claimants": [
    { "id": "org-001", "name": "武汉智造云谷科技有限公司", "value": 8 }
  ],
  "claimantLinks": [],
  "ipcDist": [
    { "name": "G06V", "value": 42 }
  ],
  "activities": []
}
```

可参考现有接口：

- `GET /api/patents/list`

需要后端补专利详情数据，或由 `/api/space/intel/{itemId}?category=Patent` 聚合返回。

## 5. 需要确认

1. 区域聚合结果能否返回 `regionCode`。如果暂时不能，请至少返回 `province`、`city`、`district`。
2. `Paper`、`Patent` 是否能支持按 `regionCode` 查询列表。
3. 企业是否能提供详情数据，或由 `/api/space/intel/{itemId}?category=Enterprise` 聚合返回。

