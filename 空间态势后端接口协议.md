# 空间态势后端接口协议

适用项目：`C:\work\yc-demo` 当前空间态势页面。地图边界、行政区层级、三维渲染、粒子视觉由前端处理；后端只需要提供业务数据。

## 接口总览

| 页面位置 | 接口 | 用途 |
| --- | --- | --- |
| 全国/省/市地图着色、顶部统计 | `GET /space/regions/summary` | 返回区域统计汇总和下级区域统计 |
| 点击城市后的粒子列表 | `GET /space/regions/{regionCode}/intel` | 返回人才、企业、论文、专利列表 |
| 点击单个粒子详情面板 | `GET /space/intel/{itemId}` | 返回单个人才/企业/论文/专利详情 |
| 省份/城市榜单面板 | `GET /space/rankings/regions` | 返回区域排名 |
| 产业链筛选下的空间统计 | `GET /space/chains/stats` | 返回某产业链或节点在各地区的分布 |

通用约定：

- `regionCode/adcode` 使用国家统计行政区划编码。
- 全国用 `100000`，省级如 `340000`，市级如 `341100`。
- 分类枚举：`Talent` 人才、`Enterprise` 企业、`Paper` 论文/期刊、`Patent` 专利。
- GET 接口没有请求体，下面的“请求结构”指 query/path 参数。

## 1. 区域统计汇总

对应位置：

- 全国地图：每个省的颜色深浅、热点高亮、顶部总数。
- 省级地图：省内每个城市的颜色深浅、热点高亮。
- 市级选中：当前城市四类数据总数。

接口：

```http
GET /space/regions/summary
```

请求结构：

| 参数 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `regionCode` | 否 | string | `100000` | 不传或 `100000` 表示全国；传省级返回省内城市；传市级返回该市汇总 |
| `level` | 否 | string | `province` | 返回下级粒度：`province`、`city`；不传可由后端按 `regionCode` 判断 |
| `category` | 否 | string | `Enterprise` | 只看某类数据；不传返回全部 |
| `year` | 否 | number | `2026` | 统计年份；不传默认最新 |

请求示例：

```http
GET /space/regions/summary?regionCode=100000&level=province
GET /space/regions/summary?regionCode=340000&level=city
```

响应结构：

```json
{
  "regionCode": "340000",
  "regionName": "安徽省",
  "level": "city",
  "updatedAt": "2026-05-09T10:00:00+08:00",
  "totals": {
    "Talent": 12800,
    "Enterprise": 6200,
    "Paper": 18000,
    "Patent": 52000
  },
  "regions": [
    {
      "adcode": "341100",
      "name": "滁州市",
      "level": "city",
      "parentAdcode": "340000",
      "score": 88.5,
      "trend": "+8.4%",
      "counts": {
        "Talent": 820,
        "Enterprise": 430,
        "Paper": 1260,
        "Patent": 3600
      },
      "highlights": ["高端装备", "智能制造"]
    }
  ]
}
```

字段用途：

| 字段 | 前端用途 |
| --- | --- |
| `totals` | 顶部统计卡片、当前区域概览 |
| `regions[].adcode` | 和前端本地地图数据匹配 |
| `regions[].score` | 地图颜色深浅、排序 |
| `regions[].counts` | 地区四类数据数量 |
| `regions[].trend` | 趋势标签，可选 |
| `regions[].highlights` | 地区标签，可选 |

## 2. 区域情报列表

对应位置：

- 点击某个城市后，生成空间态势里的粒子点。
- 左右侧列表展示该城市的人才、企业、论文、专利。
- 当前前端至少需要每条数据有名称、分类、摘要、标签、热度值。

接口：

```http
GET /space/regions/{regionCode}/intel
```

请求结构：

| 参数 | 位置 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `regionCode` | path | 是 | string | `341100` | 当前城市或区域 adcode |
| `category` | query | 否 | string | `Talent` | `Talent/Enterprise/Paper/Patent`；不传返回全部 |
| `page` | query | 否 | number | `1` | 页码 |
| `pageSize` | query | 否 | number | `20` | 每页数量 |
| `keyword` | query | 否 | string | `机器人` | 名称、机构、关键词搜索 |
| `year` | query | 否 | number | `2026` | 年份筛选 |

请求示例：

```http
GET /space/regions/341100/intel?category=Talent&page=1&pageSize=20
```

响应结构：

```json
{
  "regionCode": "341100",
  "regionName": "滁州市",
  "category": "Talent",
  "page": 1,
  "pageSize": 20,
  "total": 128,
  "items": [
    {
      "id": "talent-341100-0001",
      "category": "Talent",
      "name": "张三",
      "title": "智能制造关键技术带头人",
      "subtitle": "滁州智能装备创新中心",
      "value": 96,
      "trend": "+11%",
      "tags": ["高层次人才", "论文 18 篇", "专利 6 件"]
    }
  ]
}
```

四类数据字段口径：

| 分类 | `name` | `title` | `subtitle` | `tags` 示例 |
| --- | --- | --- | --- | --- |
| `Talent` | 人才姓名 | 职称/方向 | 所属机构 | 高层次人才、论文数、专利数 |
| `Enterprise` | 企业名称 | 企业类型/链上角色 | 所在地区/产业节点 | 专精特新、上市公司、高新技术企业 |
| `Paper` | 论文题名 | 期刊/会议 | 作者或机构 | SCI、被引次数、关键词 |
| `Patent` | 专利名称 | 专利类型 | 申请人/IPC | 发明专利、授权、PCT |

字段用途：

| 字段 | 前端用途 |
| --- | --- |
| `id` | 点击粒子后查询详情 |
| `category` | 粒子颜色和分类切换 |
| `name/title/subtitle` | 粒子卡片和列表文案 |
| `value` | 粒子大小、热度排序 |
| `trend` | 趋势标签，可选 |
| `tags` | 卡片标签 |

## 3. 单个情报详情

对应位置：

- 点击人才/企业/论文/专利粒子后，右侧详情面板。
- 首版可以只返回通用详情；图表数据可以后续扩展。

接口：

```http
GET /space/intel/{itemId}
```

请求结构：

| 参数 | 位置 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `itemId` | path | 是 | string | `talent-341100-0001` | 列表接口返回的 id |
| `category` | query | 是 | string | `Talent` | `Talent/Enterprise/Paper/Patent` |

请求示例：

```http
GET /space/intel/talent-341100-0001?category=Talent
```

响应结构：

```json
{
  "id": "talent-341100-0001",
  "category": "Talent",
  "name": "张三",
  "title": "智能制造关键技术带头人",
  "subtitle": "滁州智能装备创新中心",
  "summary": "长期从事机器视觉、智能检测与制造装备方向研究。",
  "metrics": [
    { "label": "综合热度", "value": "96" },
    { "label": "成果数量", "value": "24" },
    { "label": "合作节点", "value": "18" },
    { "label": "增长率", "value": "+11%" }
  ],
  "activities": [
    {
      "type": "paper",
      "tag": "论文",
      "date": "2026-04-18",
      "title": "区域智能装备协同网络研究",
      "impact": "正向"
    }
  ],
  "relations": [
    {
      "id": "enterprise-341100-0003",
      "category": "Enterprise",
      "name": "滁州智能装备有限公司",
      "relationType": "任职机构"
    }
  ]
}
```

可选图表字段：

| 分类 | 可选字段 | 用途 |
| --- | --- | --- |
| `Talent` | `graphNodes`、`graphLinks`、`directions`、`pubTrend` | 人才合作图、方向、成果趋势 |
| `Enterprise` | `techTags`、`talentTypes`、`outputTrend`、`partners` | 企业技术标签、人才结构、成果趋势、合作方 |
| `Paper` | `keywords`、`authors`、`relatedPapers`、`citationTrend` | 论文关键词、作者、相关论文、引用趋势 |
| `Patent` | `techTree`、`filingTrend`、`claimants`、`ipcDist` | 专利技术树、申请趋势、申请人、IPC 分布 |

## 4. 省份/城市榜单

对应位置：

- 空间态势分析面板里的省份/城市排行榜。
- 如果当前页面不展示榜单，可以作为二期接口。

接口：

```http
GET /space/rankings/regions
```

请求结构：

| 参数 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `scope` | 否 | string | `country` | `country` 全国榜；`province` 省内城市榜 |
| `parentCode` | 否 | string | `340000` | 查某省内城市榜时传省级 adcode |
| `level` | 否 | string | `city` | `province/city` |
| `category` | 否 | string | `overall` | `overall/Talent/Enterprise/Paper/Patent` |
| `limit` | 否 | number | `20` | 返回条数 |

请求示例：

```http
GET /space/rankings/regions?scope=province&parentCode=340000&level=city&category=overall&limit=20
```

响应结构：

```json
{
  "scope": "province",
  "parentCode": "340000",
  "level": "city",
  "category": "overall",
  "items": [
    {
      "rank": 1,
      "adcode": "341100",
      "name": "滁州市",
      "parentAdcode": "340000",
      "parentName": "安徽省",
      "score": 88.5,
      "counts": {
        "Talent": 820,
        "Enterprise": 430,
        "Paper": 1260,
        "Patent": 3600
      },
      "trend": "+8.4%"
    }
  ]
}
```

## 5. 产业链筛选统计

对应位置：

- 后续如果空间态势和“产业链全景”联动，选择某条产业链或某个节点后，需要看它在全国各地区的分布。
- 当前空间态势首版如果没有产业链筛选，此接口可以二期接。

接口：

```http
GET /space/chains/stats
```

请求结构：

| 参数 | 必填 | 类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| `chain` | 否 | string | `电子湿化学材料` | 产业链名称 |
| `nodeName` | 否 | string | `硫酸` | 产业链节点名称 |
| `regionCode` | 否 | string | `100000` | 不传或全国返回省级分布；传省返回城市分布 |
| `level` | 否 | string | `province` | `province/city` |
| `category` | 否 | string | `Enterprise` | `Talent/Enterprise/Paper/Patent`；不传返回全部 |

请求示例：

```http
GET /space/chains/stats?chain=电子湿化学材料&nodeName=硫酸&regionCode=100000&level=province
```

响应结构：

```json
{
  "chain": "电子湿化学材料",
  "nodeName": "硫酸",
  "regionCode": "100000",
  "level": "province",
  "totals": {
    "Talent": 260,
    "Enterprise": 118,
    "Paper": 430,
    "Patent": 960
  },
  "regions": [
    {
      "adcode": "340000",
      "name": "安徽省",
      "score": 82.3,
      "counts": {
        "Talent": 18,
        "Enterprise": 12,
        "Paper": 40,
        "Patent": 96
      }
    }
  ]
}
```

## 6. 当前前端接口文件建议

当前文件：[space.js](C:/work/yc-demo/demo/src/api/space.js)

现有接口：

```js
fetchLocationIndex()
fetchRegionGeo(code)
fetchRegionIntel(code, category)
```

建议调整：

```js
fetchRegionSummary(params)
fetchRegionIntel(regionCode, params)
fetchIntelDetail(itemId, params)
fetchRegionRankings(params)
fetchChainSpaceStats(params)
```

说明：

- `fetchRegionGeo` 不作为必须后端接口，地图 GeoJSON 前端本地已有。
- `fetchLocationIndex` 也不是必须接口，行政区树前端本地已有。
- 首版最关键的是 `fetchRegionSummary` 和 `fetchRegionIntel`。

## 7. 后端优先级

1. `GET /space/regions/summary`：没有它，全国/省/市地图只能用假数据。
2. `GET /space/regions/{regionCode}/intel`：没有它，城市粒子和列表只能用假数据。
3. `GET /space/intel/{itemId}`：决定详情面板是否接真实数据。
4. `GET /space/rankings/regions`：榜单分析增强。
5. `GET /space/chains/stats`：产业链联动增强。
